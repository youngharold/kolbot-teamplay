/**
*  @filename    TeamGameCoordinator.js
*  @desc        Leader owns game name / pass / difficulty. Replaces per-char game naming.
*               PR-1: stub. Implementation lands in PR-3.
*/

(function (module) {
	module.exports = {
		// TODO(PR-3): nextGameName(), gamePass(), difficulty()
		nextGameName: function () { return "tp-stub"; },
		gamePass: function () { return "stub"; },
		difficulty: function () { return 0; }
	};
})(module);

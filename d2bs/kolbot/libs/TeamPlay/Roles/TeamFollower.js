/**
*  @filename    TeamFollower.js
*  @desc        In-game follower driver. Listens to leader's broadcasts; transit/engage/retreat state machine.
*               PR-1: stub. Implementation lands in PR-5.
*/

(function (module) {
	module.exports = {
		// TODO(PR-5): run() — main follower loop; move to leader, help attack, retreat/heal as needed.
		run: function () {}
	};
})(module);

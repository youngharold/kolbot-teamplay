/**
*  @filename    TeamTristram.js
*  @desc        MVP-1 core script — Stony Field WP → Cain stones → Tristram portal → team clear → exit.
*               PR-1: stub. Implementation lands in PR-6.
*/

(function (module) {
	module.exports = {
		// TODO(PR-6): run() — leader drives, broadcasts phase events, exits on Griswold kill.
		run: function () {}
	};
})(module);

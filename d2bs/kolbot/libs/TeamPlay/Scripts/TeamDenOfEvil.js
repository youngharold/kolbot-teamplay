/**
*  @filename    TeamDenOfEvil.js
*  @desc        Personal quest: each char clears Den of Evil for skill point reward.
*               PR-1: stub. Implementation lands in PR-7.
*/

(function (module) {
	module.exports = {
		// TODO(PR-7): run() — leader clears, each follower also TPs in and gets personal-quest credit.
		run: function () {}
	};
})(module);
